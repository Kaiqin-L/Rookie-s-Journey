*** Set（）***
  创建一个无序不重复元素集，可进行关系测试，删除重复数据，还可以计算交集、差集、并集等。
  
  例如 a = [1, 2, 3, 4, 4, 6],  可通过Set（a）创建集合，返回的是set类型，也就是集合类型， set([0, 1, 2, 4, 6])。该集合中的元素值4不会出现2次。
  
  
  注：集合（set）是一个无序的不重复元素序列，可以通过{ }或set（）函数来创建。值得注意的是，创建空集合时必须用set（）而不是{ }，因为{ }是用来创建一个空字典。
  集合也有诸如添加，删除元素等许多内置方法，重要的是它提供了求集合交集，差集，俩集合是否包含先共同元素，
