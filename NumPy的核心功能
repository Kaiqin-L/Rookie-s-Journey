(1) a=np.array([[],
           []])
           中括号里面是同质元素
           
(2) a.ndim是获得数组的维度，也就是轴（可以通过中括号的层数），单个数的维度为0，例如np.array(1)

(3) a.shape是求解数组的形状大小，比如一个n行m列的数组，它的shape是（n,m）

(4) a.dtype是求解数组中元素的数据类型

(5) a.size是求解数组中元素的个数

(6) a.itemsize是求解数组中的数据类型的字节数，例如int32的itemsize是4=32/8

U,S,V=np.linalg.svd(A,full_matrices=False)
跟U,S,V=np.linalg.svd(A)是不一样的
